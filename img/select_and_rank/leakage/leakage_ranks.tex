\documentclass{standalone}
\input{../../_common.tex}

\begin{document}
\begin{tikzpicture}
    \begin{axis}[
            ybar,
            width=10cm,
            bar width=0.6,
            ymode=log,
            xlabel={Rank of leakage sentence in selection},
            ylabel={Number of documents},
            xtick align=inside,
            grid=major,
        ]
        \addplot+[
            plotColor3*,
            draw=black,
        ] table[
                col sep=comma,
                x=rank,
                y=num,
            ] {leakage_all_ranks.csv};
    \end{axis}
\end{tikzpicture}
\end{document}
